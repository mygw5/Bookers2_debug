<div class="container">
  <div class="row">
    <h3>送信完了しました</h3>
    <p>内容は以下の通りです。</p>
    <div>
      [タイトル]
      <p class="font-weight-bold"><%= @mail_title %></p>
    </div>
    <div>
      [本文]
      <p class="font-weight-bold"><%= safe_join(@mail_content.split("\n"), tag(:br)) %></p>
    </div>
  </div>
</div>